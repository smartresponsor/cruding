# Symfony routing remains only a delivery layer. Cruding owns CRUD grammar and
# loads route definitions through a custom loader so var/cruding manifests stay
# optional, removable runtime artifacts instead of required host config imports.
cruding:
    resource: .
    type: cruding
